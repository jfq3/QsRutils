% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_primer_hits.R
\name{check_primer_hits}
\alias{check_primer_hits}
\title{Check Primer Hits}
\usage{
check_primer_hits(
  path,
  fwd_pattern = "_R1.fastq",
  rev_pattern = "_R2.fastq",
  fwd_primer = "GGAAGTAAAAGTCGTAACAAGG",
  rev_primer = "GCTGCGTTCTTCATCGATGC"
)
}
\arguments{
\item{path}{Path to paired sequence files}

\item{fwd_pattern}{Portion of file name that distinguishes forward read files. The default is "_R1.fastq"}

\item{rev_pattern}{Portion of the file name that distinguishes the reverse file. The default is "_R2.fastq"}

\item{fwd_primer}{Nucleotide sequence of the forward primer}

\item{rev_primer}{Nucleotide sequence of the reverse primet}
}
\value{
A table of hits to the sequences by all primer orientations
}
\description{
Determine hits of all orientations of primers to paired sequence files.
}
\details{
This function is for checking the effectiveness of primer trimming of ITS sequences.
Because the ITS region varies in length, it is possible for forward sequences to extend past the reverse primenr region and vice-versa, leadsing to what Robert Edgar calls staggered pairs fi the sequences are merged.

The fwd_pattern and rev_pattern must contain the file extension. The defaults are "_R1.fastq" and "_R2.fastq".

Default primers are ITS5 (forward) and ITS2 (reverse) from White et.al 1990.

ITS5: "GGAAGTAAAAGTCGTAACAAGG"

ITS2: "GCTGCGTTCTTCATCGATGC"
}
\examples{
\dontrun{
  # This takes a while and requires fastq files
  path <- system.file("extdata", package = "QsRutils")
  check_primer_hits(
    path = path,
    fwd_pattern = "raw_1.fastq.gz",
    rev_pattern = "raw_2.fastq.gz",
    fwd_primer = "GGAAGTAAAAGTCGTAACAAGG",
    rev_primer = "GCTGCGTTCTTCATCGATGC"
  )
}
}
