# avg_alpha errors on insufficient row sums

    Code
      avg_alpha(BCI, sampling_depth = 1e+09, iterations = 5)
    Condition
      Error in `avg_alpha()`:
      ! All samples must have at least 'sampling_depth' counts (min row sum < sampling_depth).

